# Web scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Date parsing
python-dateutil>=2.8.0

# LLM relevance filter (set OPENAI_API_KEY before running any scraper)
openai>=1.30.0

# Note: SQLite is in Python's standard library — no install needed
